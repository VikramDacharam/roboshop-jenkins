- name: Ansible playbook to create all roboshop jobs
  hosts: localhost
  roles:
    - jenkins-jobs
  vars:
    folders:
      - APP-SETUP-WITH-ANSIBLE
    pipeline_jobs:
      - { jobName: "roboshop", folderName: "demo", repoName: "roboshop-ansible"}

